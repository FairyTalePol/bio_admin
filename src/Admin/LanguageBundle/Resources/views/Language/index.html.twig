{% extends app.request.isXmlHttpRequest ? 'AdminClientBundle::ajax.html.twig' : app.user and is_granted('IS_AUTHENTICATED_FULLY') ? 'AdminClientBundle::layout.html.twig' : 'AdminClientBundle::default.html.twig' %}

{% block body %}

  <table class="table table-hover">
    <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Locale</th>
      <th>&nbsp;</th>
    </tr>
    </thead>
    <tbody>
    {% for language in languages %}
      <tr>
        <td>{{ language.id }}</td>
        <td>{{ language.name }}</td>
        <td>{{ language.locale }}</td>
        <td>
          <nobr>
            <form id="edit-language-btn" style="display: inline-block;"
                  action="{{ path('language_edit', {id: language.id}) }}"
                  enctype="application/x-www-form-urlencoded" method="get">
              <button type="submit" class="btn btn-info"><span class="glyphicon glyphicon-edit"></span></button>
            </form>
            <form style="display: inline-block;" action="{{ path('language_delete', {id: language.id}) }}"
                  enctype="application/x-www-form-urlencoded" method="get">
              <button type="submit" class="btn btn-danger"><span class="glyphicon glyphicon-remove"></span></button>
            </form>
          </nobr>
        </td>
      </tr>
    {% endfor %}
    </tbody>
  </table>

  <a href="{{ path('language_add') }}" id="add-language-btn" class="btn btn-primary">Добавить</a>

  <div id="modal" class="modal fade">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title">Modal title</h4>
        </div>
        <div class="modal-body">
          <p>One fine body&hellip;</p>
        </div>
      </div>
      <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
  </div><!-- /.modal -->

{% endblock %}